import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-angular-testapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
